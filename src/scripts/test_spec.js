'use strict';

describe('Test', function () {

    it('should have the component class', function () {
        debugger
        var ex = expect(1);
        expect(!!ex.toHaveClass).toBeTruthy();
    });

});
